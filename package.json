{
  "dependencies": {
    "canvas": "^3.1.0",
    "fast-glob": "^3.3.3",
    "slugify": "^1.6.6"
  },
  "scripts": {
    "dev": "cp static/admin/config.dev.yml static/admin/config.yml && hugo serve",
    "build": "node generate-gradient.js && cp static/admin/config.prod.yml static/admin/config.yml && hugo --gc --minify"
  }
}
